[project]
name = "models"
version = "0.1.0"
description = "GGUF scanner and calculator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gguf>=0.18.0",
    "tqdm>=4.67.3",
]

[dependency-groups]
dev = [
    "pyright>=1.1.409",
    "ruff>=0.15.12",
]
